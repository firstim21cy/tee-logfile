[project]
name = "tee-logfile"
version = "0.1.0"
description = ""
authors = [
    {name = "cheny",email = "cheny@fcc.ca"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
]

[tool.poetry]
packages = [{include = "tee_logfile", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
